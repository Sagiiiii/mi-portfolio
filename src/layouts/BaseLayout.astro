---
import Layout from './Layout.astro';

interface Props {
  titulo: string;
  descripcion?: string;
}

const { titulo, descripcion } = Astro.props;
---

<Layout title={`${titulo} | Sagiii.dev`} description={descripcion}>
  <slot />
</Layout>
